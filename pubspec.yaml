name: faisal_alanazi_proj1
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  carousel_slider: ^5.1.2
  dots_indicator: ^4.0.1
  flutter:
    sdk: flutter
  flutter_dotenv: ^6.0.1
  flutter_rating_bar: ^4.0.1
  google_fonts: ^6.2.1
  supabase_flutter: ^2.17.2

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - .env

flutter_assets:
  assets_path: assets/images
  output_path: lib/constants/gen
  filename: assets.dart
